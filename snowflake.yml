definition_version: 1
snowpark:
  project_name: "hol"
  stage_name: "analytics.deployment"
  src: "app/"
  functions:
    - name: "app"
      database: "snowpark_app"
      schema: "snowparkapp_schema"
      handler: "main.main"
      runtime: "3.10"
      # signature:
      #   - name: "temp_f"
      #     type: "float"
      returns: float